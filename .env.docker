# --- DATABASE ---
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=31102006
DB_NAME=floodai

# --- EXTERNAL SERVICES ---
SEGMENT_SERVICE_URL=http://worker:8080/inference
VLM_SERVICE_URL=http://ollama:11434/reasoning
GATEWAY_BASE_URL=http://localhost:5000
USE_LOCAL_VLM=true

# --- OLLAMA (Local VLM) ---
# Biến này được dùng bởi thư viện Ollama JS SDK
OLLAMA_HOST=http://ollama:11434

# --- STORAGE ---
# Thư mục uploads trong container
UPLOADS_DIR=/app/uploads
